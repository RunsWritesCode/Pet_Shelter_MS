<!-- table of owners and their pets -->

<table>
  <tr>
    <th>
      Owner
    </th>
    <th>
      Pet
    </th>
  </tr>
  <% @owners.each do |owner| %>
    <% animals = owner.animals %>
    <tr>
      <td>
        <%= owner.pretty_name %>
      </td>
      <td>
        <% if animals.length == 0 %>
          <%= "no pets" %>
        <% else %>

            <% animals.each do |animal| %>
              <p><a class="body-links" href="/animals/<%= animal.id %>"><%= animal.name() %></a></p>
            <% end %>

        <% end %>
      </td>
  </tr>

  <% end %>

</table>
